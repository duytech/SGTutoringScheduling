<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Use-case layer: the API contracts, the conflict engine and the scheduling
    services. References the Domain only - no EF Core, no ASP.NET. Persistence
    is reached through the IScheduleStore port defined here and implemented in
    Infrastructure.
  -->
  <PropertyGroup>
    <RootNamespace>TutoringScheduling.Application</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Domain\TutoringScheduling.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="TutoringScheduling.Tests" />
  </ItemGroup>

</Project>
